# 量化交易系统使用指南

## 🚀 快速开始

### 1. 命令行使用

```bash
cd /Users/gexin/.openclaw/workspace/stock-trading

# 分析股票
python3 main.py analyze AAPL

# 回测
python3 main.py backtest META --start 2024-01-01 --end 2024-12-31

# 策略迭代 (自动测试多只股票)
python3 main.py iterate META,NVDA,AMZN --start 2024-01-01 --end 2024-12-31

# 市场状态
python3 main.py status
```

### 2. OpenClaw 集成使用

在 OpenClaw 中直接调用:

```python
from openclaw_integration import analyze_stock, run_backtest, find_best_stocks

# 分析股票
result = analyze_stock('META')
print(f"META 当前价格：${result['price']}")
print(f"舆情：{result['sentiment_level']}")

# 运行回测
result = run_backtest('META', '2024-01-01', '2024-12-31')
print(f"收益率：{result['total_return']}%")

# 寻找最佳股票
result = find_best_stocks(['META', 'NVDA', 'AMZN'], '2024-01-01', '2024-12-31')
print(f"最佳股票：{result['best_result']['symbol']}")
```

### 3. LLM 决策集成

在 OpenClaw 对话中:

```
请分析 META 的当前情况，给出交易建议
```

系统会自动:
1. 获取最新市场数据
2. 计算技术指标
3. 分析舆情情绪
4. 生成 LLM 决策提示词
5. 调用 LLM 获取决策
6. 返回具体交易建议

---

## 📊 回测结果解读

### 关键指标

| 指标 | 含义 | 优秀标准 |
|------|------|----------|
| 总收益率 | 策略总收益百分比 | ≥20% |
| 最大回撤 | 最大资金下跌 | ≥-15% |
| 夏普比率 | 风险调整后收益 | ≥1.5 |
| 胜率 | 盈利交易占比 | ≥55% |
| 盈亏比 | 平均盈利/平均亏损 | ≥2.0 |

### 结果示例

```
📊 回测结果 - META
============================================================
💰 资金变化:
  初始资金：$10,000.00
  最终资金：$18,069.83
  总收益：  $8,069.83

📈 绩效指标:
  总收益率：  +80.70%  ✅
  最大回撤：  -11.47%  ✅
  夏普比率：  2.01     ✅
  胜率：      53.3%    ✅
  盈亏比：    5.63     ✅
```

---

## 🎯 推荐策略

### 当前最佳配置

**股票**: META (Meta Platforms)
**策略**: relaxed_strategy (宽松版)
**周期**: 日线级别
**仓位**: 100% (全仓)

**预期表现** (基于 2024 年回测):
- 年化收益：~80%
- 最大回撤：~-11%
- 胜率：~53%
- 交易频率：~30 次/年

### 保守配置

**股票组合**: 50% META + 50% AMZN
**预期**: 降低回撤，稳定收益

---

## ⚙️ 自定义策略

编辑 `strategies/relaxed_strategy.py`:

```python
def relaxed_strategy(row, indicators):
    buy_conditions = []
    sell_conditions = []
    
    # 自定义你的交易逻辑
    rsi = indicators.get('rsi_14')
    if rsi < 30:  # 调整阈值
        buy_conditions.append("RSI 超卖")
    
    # ... 添加更多条件
    
    if len(buy_conditions) >= 2:  # 调整确认数量
        return 'buy'
    
    return 'hold'
```

---

## 📁 输出文件

回测结果保存在 `data/` 目录:

```
data/
├── backtest_META_2024-01-01_2024-12-31.json  # 单次回测
├── iteration_results.json                     # 迭代测试
└── ...
```

JSON 格式示例:
```json
{
  "symbol": "META",
  "total_return": 80.70,
  "max_drawdown": -11.47,
  "sharpe_ratio": 2.01,
  "win_rate": 53.3,
  "trades": [...]
}
```

---

## 🔧 常见问题

### Q: 为什么某些股票没有交易信号？
A: 策略条件可能过于严格。可以:
1. 使用 `relaxed_strategy` 代替 `default_strategy`
2. 调整 RSI/MACD 阈值
3. 选择波动性更大的股票

### Q: 回测收益很高但实盘会怎样？
A: 回测≠实盘。注意:
1. 回测使用历史数据，不代表未来
2. 实盘有滑点和手续费
3. 建议先用模拟盘验证

### Q: 如何选择交易股票？
A: 参考 `RESULTS_SUMMARY.md`:
- 最佳：META (综合表现)
- 稳健：AMZN (高胜率)
- 激进：NVDA (高收益高回撤)
- 避免：AMD, MSFT, BABA

---

## 📞 技术支持

- 项目位置：`/Users/gexin/.openclaw/workspace/stock-trading/`
- 技能文档：`skills/SKILL.md`
- 结果汇总：`RESULTS_SUMMARY.md`
- 进度追踪：`PROGRESS.md`

---

**Version**: 4.0.0
**Last Updated**: 2026-02-27
